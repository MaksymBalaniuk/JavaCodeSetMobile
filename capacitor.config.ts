import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.javacodeset',
  appName: 'JavaCodeSet',
  webDir: 'dist/java-code-set',
  bundledWebRuntime: false
};

export default config;
